//! Workspace package that owns the top-level runnable examples.
//!
//! The library implementation lives in `crates/dsl`; this package exists so
//! Cargo automatically discovers every Rust source under `examples/`.
