# Group standard-library, external, and current-crate imports automatically.
# Rustfmt cannot distinguish workspace dependencies from third-party crates;
# those are both classified as external imports.
edition = "2024"
reorder_imports = true
group_imports = "Preserve"
imports_granularity = "Module"
